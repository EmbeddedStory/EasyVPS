theme: dark

server:
  host: 0.0.0.0
  port: 9091

totp:
  issuer: auth.embeddedstory.com

authentication_backend:
  file:
    path: /config/users_database.yml

access_control:
  default_policy: deny
  rules:
    # Allow API calls
    - domain: cal.embeddedstory.com
      resources:
        - ".*/api.*"
      policy: bypass
    - domain: cal.embeddedstory.com
      policy: two_factor
    - domain: knw.embeddedstory.com
      policy: two_factor
    - domain: wiki.embeddedstory.com
      policy: two_factor
    - domain: main.embeddedstory.com
      policy: two_factor

session:
  name: authelia_session
  expiration: 3600  # 1 hour
  inactivity: 3600  # 1 hour
  domain: embeddedstory.com 

regulation:
  max_retries: 3
  find_time: 120
  ban_time: 300

storage:
  local:
    path: /config/db.sqlite3

notifier:
  disable_startup_check: false
  filesystem:
    filename: /config/notification.txt
